#
# COMMENT
#
$HTTP["useragent"] =~ "A" { url.access-deny = ( "" ) accesslog.filename = "/var/log/lighttpd/ai-bots.log" }
$HTTP["useragent"] =~ "B" { url.access-deny = ( "" ) accesslog.filename = "/var/log/lighttpd/ai-bots.log" }
$HTTP["useragent"] =~ "C" { url.access-deny = ( "" ) accesslog.filename = "/var/log/lighttpd/ai-bots.log" }
